{
  "name": "project-template",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "echo 'TODO: add project-specific setup command in .devcontainer/devcontainer.json'",
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true
      },
      "extensions": [
        "EditorConfig.EditorConfig"
      ]
    }
  }
}

